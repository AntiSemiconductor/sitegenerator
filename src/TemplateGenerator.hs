module TemplateGenerator where
import Hakyll

-- templateGenerator = do
--   template <- getResourceBody
--   --x <- loadAndApplyTemplate "header" ""
--   return x
--   where
--     --templateContext = field "header" $ \item -> templates
